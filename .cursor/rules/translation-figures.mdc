---
description: 写论文译文和要点时必须从 PDF 裁图、嵌图、对着图写
globs: 文献/翻译/**/*.md
alwaysApply: false
---

# 译文和要点必须对着图写

写 `文献/翻译/*-译文.md` 或 `*-要点.md` 之前，先看 PDF 里的图，不要只盯文字。

## 出图（写译文的人做）

1. 先 `pdfimages -list`。一张 Fig 若是**一整张位图**，用 `pdfimages -png` 直接提取，不要再渲页硬裁。
2. 结构图、折线、箭头框是**矢量**：`pdftoppm` 渲该页，只裁图本身。
3. 文件名：`fig1.png`、`table1.png`。附录图号若重计，用 `app-fig1.png`。
4. **裁准。** 不要带上邻栏正文、页眉、arXiv 戳、下一节标题。双栏时只留有图的那一栏。反例：`附件/图/SPIRAL/fig1.png` 左边卷进了引言段落。
5. 只收正文图和关键表。附录效果墙不必全收。
6. 没有图，不准写「库内暂无裁图」交差。

## 译文里怎么放

图出现的那一节，写成：

```
![[图/论文名/fig1.png]]

Fig. 1：一句话说明图上是什么。和本课题有关的误读也写在这里。
```

图号跟原文。标题里不要写 `[[双链]]`。

## 要点里怎么写

必须有一节 `## 关键图`。每条写图上实际看见什么，并链到译文里放这张图的节：

```
- Fig. 1：……。图在 [[论文名-译文#3.1 小节标题]]。
```

优化步、去噪步、网格图元，不要写成课堂作画阶段。没看图不要编。
