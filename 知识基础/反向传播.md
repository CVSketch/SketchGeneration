---
tags:
  - 知识基础
---

# 反向传播

反向传播是算[[梯度]]的一种算法。

模型从输入算到输出，叫向前。  
损失算出来之后，从输出往输入倒着问：谁害损失变大的？叫反向。

倒着问的时候，用初中就有的「链式法则」：这一层的对错，按它怎么用上一层的输出，分摊回去。

你不必手算。只要知道：

1. 中间哪一步不能求导（比如硬性的「最大的那个赢」），梯度到这里就断了。
2. [[可微渲染器]]就是为了让「画到画布上」这一步不要断。

相关：[[神经网络]]、[[可微]]。
