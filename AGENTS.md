# 素描过程生成知识库约定

这个文件夹是 Obsidian 库。写给人读的中文前，先加载 `~/.codex/skills/write-normal-chinese/SKILL.md`。

Obsidian 写法以算法竞赛库里已经定下的两套语法为准，见 `XCPC/算法竞赛/.cursor/rules/markdown-two-dialects.mdc` 和 `obsidian-markdown.mdc`。本库只补论文笔记和 YAML 属性区的约定。

## 两套 Markdown，禁止混用

| 场合 | 用哪套 |
| :--- | :--- |
| 和用户对话 | 普通回复 Markdown |
| 编辑本库 `.md` | 只准用 Obsidian 那套 |

聊天里：公式用 `\(...\)` 和 `\[...\]`；链接用 `[文字](网址)`。不要写 `[[双向链接]]`、`![[嵌入]]`、Callout、`$...$` / `$$...$$`。

写库时：内链用 `[[笔记名]]`；PDF 用 `[[文件名.pdf]]`；公式用 `$...$` / `$$...$$`；流程图用 Mermaid。不要写 `\(...\)`、`\[...\]`、`\texttt{}`。

## YAML 属性区（必须能解析）

文首 `---` 之间是 YAML，不是随便写的标签。Obsidian 解析失败，整页属性都会红。

**值里只要出现「英文冒号 + 空格」，必须加双引号。** 卷期页码最容易踩。

错：

```yaml
venue: 图学学报, 47(2): 380-389
```

对：

```yaml
venue: "图学学报, 47(2): 380-389"
```

同样要加引号的情况：

- `title` 里有英文冒号，例如 `StrokeFusion: Vector Sketch ...`
- `pdf` 写双链，例如 `"[[StrokeFusion.pdf]]"`
- `arxiv` 写成字符串，例如 `"2503.23752"`

没有就留空键，不要编。写完属性区后，确认这段 YAML 能解析，再往下写正文。

论文笔记字段跟 `模板/论文笔记.md`：`title`、`short`、`authors`、`year`、`venue`、`ccf`、`arxiv`、`pdf`、`code`、`tags`、`status`。

## 写论文笔记

- 一篇论文一篇笔记。先复制模板，再改字段。
- 入口是 `00-首页.md`。新笔记要挂到首页线索、`研究/20-论文索引.md`。缺 PDF 记到 `附件/待补下载.md`。
- 摘要自己写，不要机翻腔。没读到的全文细节不要编。
- 本课题看过程生成。不要把成品草图扩散写成阶段扩散。

## 目录

- `文献/油画过程/`、`文献/素描生成/`、`文献/深大相关/`、`文献/项目/`
- `文献/翻译/`：`论文名-译文.md` 是按章节写完的全文译文；`论文名-要点.md` 每条链到译文某一节
- `文献/翻译/意译/`：第一轮旧意译，只留底
- `文献/不读/`：本期明确不读
- `文献/参考文献/`：从精读论文参考文献里扒下来的笔记
- `研究/`：路线、索引、任务定义、组会、复现说明、[[全文翻译约定]]
- `知识基础/`：一词一页。目录里分成低级 / 高级，文件先不挪，避免断链
- `附件/pdf/`：顶会公开原文；参考文献 PDF 放 `附件/pdf/参考文献/`
- `附件/图/`：从 PDF 裁的图，只留译文和笔记里用到的。写法：`![[图/论文名/fig1.png]]`。写全文译文前必须先裁正文 Fig / Table，禁止「只标图号、不放图」。
- `模板/论文笔记.md`、`模板/论文全文译文.md`、`模板/论文要点.md`、`模板/项目笔记.md`

第一轮只收计算机顶会公开全文。不精读 PDF-Sketch 和两篇 PaintFlow。

写全文译文时跟 [[全文翻译约定]]。不要整段粘英文，但每一节都要写到，方便从要点点进去。
